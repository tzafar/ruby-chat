<% flash.each do |message_type, message| %>
  <div class="ui <%= message_type %> message">
    <i class="close icon"></i>
    <div class="header">
      <%= message_type %>
    </div>
    <p><%= message %></p>
  </div>
<% end %>
